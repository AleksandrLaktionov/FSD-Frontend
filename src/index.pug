doctype html
html(lang='en')
    head
        meta(name="viewport" content="width=device-width, initial-scale=1.0")
        title test
    body
        div(id="root")
            include /pug/UIkit/Colors_Type.pug
            hr
            include /pug/UIkit/Form_Elements.pug